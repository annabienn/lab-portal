<div class="message <%= message.user_id == current_user_id ? "own" : "other" %>">
  <div class="message-meta">
    <strong><%= message.user.email %></strong>
    <span><%= message.created_at.strftime("%H:%M") %></span>
  </div>
  <div class="message-body"><%= message.body %></div>
</div>
